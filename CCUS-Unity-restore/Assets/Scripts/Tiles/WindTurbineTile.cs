using UnityEngine;

public class WindTurbineTile : Tile
{
    //Switches tile to visually inactive state
    // public override void UpdateIsTileVisuallyActive(){
    //     isActive = enoughResourcesToFunction;

    //     base.SetTileVisuallyActive(isActive);

    //     //Stops / starts spinning turbine depending on whether or not the tile is in its active state.
    //     SpinObject turbineSpinner = GetComponentInChildren<SpinObject>();
    //     if(turbineSpinner != null){
    //         turbineSpinner.IsSpinning = isActive;
    //     }
    // }
}
