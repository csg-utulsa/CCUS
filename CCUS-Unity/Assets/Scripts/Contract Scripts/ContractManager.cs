/*
 * Manages placing contracts in the UI, updating their status 
 * 
 */ 
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContractManager : MonoBehaviour
{
    public List<ContractData> activeContracts; // List containing every contract currently active
    public List<ContractData> availableContracts; // List of all contracts available in the level

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
